#!/usr/bin/expect -f

set timeout -1

# Get password
set namespace [lindex $argv 0]
set password [lindex $argv 1]

spawn env NAME=$namespace TYPE=validator NET=mainnet make install

expect "Are you sure"
send "y\r"

expect "Enter password"
send "$password\r"

expect "Confirm password"
send "$password\r"

expect "Are you sure"
send "y\r"

expect "Are you sure"
send "y\r"

expect eof
